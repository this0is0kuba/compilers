public class Class{
    def funkcja(arg : Int) : String = {
        val x = "test";
        val y = List(Class(1,2,3), Class(4,5,6));

        if(x == "test") {
            funckja(2);
        }
        else {
            var a = 10;
            var b = 20;
        }

        val people = List(
            "Bill",
            "Candy",
            "Karen",
            "Leo",
            "Regina"
        );

        for (p <- people) {
            for(p2 <- people) {
                println(p);
            }
        }
        return x;
    }
}